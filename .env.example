# This project calls the Anthropic API directly from the client.
# In a real deployment, proxy this through your own backend and
# keep your API key server-side — never ship it in frontend code.
ANTHROPIC_API_KEY=your_key_here
